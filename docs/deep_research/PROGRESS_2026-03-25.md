# Deep Research 功能开发进度记录

> 更新日期: 2026-03-26
> 分支: `feature/deep-research`
> 状态: **进行中 - graph 同步执行超时问题排查中**

---

## 一、已完成的组件

### 1. 核心模块

| 文件 | 功能 | 状态 |
|------|------|------|
| `app/ai/deep_research/state.py` | 状态定义 | ✅ 完成 |
| `app/ai/deep_research/nodes.py` | 节点函数 (create_analysts, generate_question, search_web, search_bocha, generate_answer, write_section, write_report 等) | ✅ 完成 |
| `app/ai/deep_research/graph_builder.py` | 图构建器 (主图 + 访谈子图) | ✅ 完成 |
| `app/ai/deep_research/prompts.py` | 提示词模板 | ✅ 完成 |
| `app/ai/deep_research/llm.py` | LLM 封装 (`@lru_cache` 单例) | ✅ 完成 |
| `app/ai/deep_research/tools/tavily.py` | Tavily 搜索工具 | ✅ 完成 |
| `app/ai/deep_research/tools/bocha.py` | Bocha 搜索工具 | ✅ 完成 |
| `app/ai/deep_research/config.py` | 配置管理 | ✅ 完成 |

### 2. API 层

| 文件 | 功能 | 状态 |
|------|------|------|
| `app/api/v1/endpoints/deep_research.py` | REST API 端点 | ✅ 完成 |
| `app/schemas/deep_research.py` | Pydantic Schema | ✅ 完成 |
| `app/services/deep_research_service.py` | 业务逻辑服务 | ✅ 完成 |

### 3. 数据库

| 文件 | 功能 | 状态 |
|------|------|------|
| `app/models/research_task.py` | ResearchTask 模型 | ✅ 完成 |
| `alembic/versions/xxx_add_research_tasks.py` | 迁移脚本 | ✅ 完成 |

### 4. 测试

| 文件 | 类型 | 状态 |
|------|------|------|
| `tests/ai/deep_research/test_api.py` | API 集成测试 (8个) | ✅ 通过 |
| `tests/ai/deep_research/test_service.py` | Service 层单元测试 (12个) | ✅ 通过 |
| `test_e2e.py` | 端到端测试脚本 | ⚠️ 待调试 |

---

## 二、工作流程图

```
[START] → [create_analysts] → [human_feedback] ─┬─→ [conduct_interview] × N (并行)
                                               │         │
                                               │    ┌───┴───┐
                                               │    │       │
                                               │ [ask_question] → [search_web] ─┐
                                               │ [ask_question] → [search_bocha]─┤
                                               │                              [answer_question]
                                               │                                   │
                                               │    ┌──────────────────────────────┘
                                               │    │
                                               │ [route_messages] → ask_question / save_interview
                                               │                                  │
                                               │                           [write_section]
                                               │
                                               └─→ [write_report]
                                                   [write_introduction]
                                                   [write_conclusion]
                                                        │
                                                   [finalize_report] → [END]
```

---

## 三、已解决的问题 (Debug Log)

| # | 问题 | 原因 | 解决方案 |
|---|------|------|----------|
| 1 | `'list' object has no attribute 'get'` | LLM 返回 `[{...}]` 而非 `{"analysts": [...]}` | 使用 `with_structured_output(Perspectives)` 强制格式 |
| 2 | `KeyError: 'affiliation'` | LLM 返回错误字段名 (如 `expertise`, `focus`) | 更新 prompt 明确要求 `name, affiliation, role, description` |
| 3 | `AttributeError: 'str' object has no attribute 'get'` | `JsonOutputParser` 返回字符串非字典 | 改用 `with_structured_output(SearchQuery)` |
| 4 | `JSONDecodeError` | LLM 输出不是合法 JSON | `with_structured_output` 内部处理解析 |
| 5 | `ValueError: Got unsupported message type: ('user', ...)` | `get_buffer_string()` 需要 `BaseMessage` 对象 | 使用 `HumanMessage(content=...)` 而非元组 |
| 6 | `Deserializing unregistered type Analyst` | `MemorySaver` 无法序列化 Pydantic 对象 | 使用 `.model_dump()` 转为字典 |
| 7 | `LangGraphDeprecatedSinceV10` | `Send` 从 `langgraph.constants` 导入 | 改为从 `langgraph.types` 导入 |
| 8 | `OPENAI_API_KEY not set` | dotenv 未加载 | 确保 `.env` 存在且 `main.py` 加载它 |
| 9 | 事件结构检查错误 | `astream_events` 格式是 `{"event":..., "name":...}` | 使用 `event.get("name", "")` |

---

## 四、当前问题

### Issue #1: API 返回空 `final_report`

**现象:**
- 直接执行 graph: 正常工作 → 2 sections, 1795 字符报告
- 通过 API 调用: `status: completed` 但 `final_report: null`

**可能原因:**
- `MemorySaver` checkpointer 与 `interrupt_before=["human_feedback"]` 组合导致状态未正确传播
- API 层的 SSE 流式处理可能丢失最终状态

**排查方向:**
1. 检查 `service.py` 中 `run_research` 的 `final_state` 获取逻辑
2. 考虑临时移除 `MemorySaver` 测试是否为 checkpointer 问题
3. 对比直接调用 `graph.invoke()` vs API 调用的状态差异

**相关代码 (`graph_builder.py:102-106`):**
```python
memory = MemorySaver()
return builder.compile(
    interrupt_before=["human_feedback"],
    checkpointer=memory
)
```

---

## 五、下一步计划

### 优先级 1: 修复 API `final_report` 为空问题
1. 在 `service.py` 添加调试日志，输出每步的 state values
2. 验证 `final_state = research_graph.get_state(config)` 是否正确获取最终状态
3. 如需要，临时移除 `MemorySaver` 检查是否为序列化问题

### 优先级 2: 完成端到端测试
1. 修复 API 问题后，运行 `test_e2e.py` 验证完整流程
2. 确认 SSE 事件流正确返回 `final_report`

### 优先级 3: 完善错误处理
1. 添加网络超时处理
2. 添加搜索 API 失败后的降级逻辑
3. 添加 LLM 调用异常处理

---

## 六、关键文件路径

```
app/ai/deep_research/
├── __init__.py
├── state.py          # InterviewState, ResearchGraphState
├── nodes.py          # 所有节点函数
├── graph_builder.py  # build_interview_subgraph, build_research_graph
├── prompts.py        # 提示词模板
├── llm.py            # get_llm()
├── config.py         # get_config()
├── tools/
│   ├── tavily.py     # get_tavily_tool()
│   └── bocha.py      # BochaSearchTool
└── service.py        # DeepResearchService

tests/ai/deep_research/
├── test_api.py       # API 集成测试
└── test_service.py   # Service 单元测试

test_e2e.py           # 端到端测试脚本
```

---

## 七、测试命令

```bash
# 运行单元测试
pytest tests/ai/deep_research/ -v

# 运行 API 集成测试
pytest tests/ai/deep_research/test_api.py -v

# 运行端到端测试 (需先启动服务)
python main.py  # 终端1
python test_e2e.py  # 终端2

# 直接测试 graph (无需启动服务)
cd .worktrees/feature-deep-research
python -c "
from app.ai.deep_research.graph_builder import research_graph
result = research_graph.invoke({'topic': 'LangGraph analysis', 'max_analysts': 2, 'human_analyst_feedback': ''})
print(f'Sections: {len(result.get(\"sections\", []))}')
print(f'Report: {len(result.get(\"final_report\", \"\"))} chars')
"
```
