"""
讲05 | Few-Shot与CoT - 实践练习参考实现

本文件包含第5讲的4个练习题的参考答案，展示如何运用：
- Few-Shot Learning示例设计
- Chain of Thought推理技术
- Few-Shot与CoT的组合应用
- 动态示例选择策略
"""

# ============================================================
# 练习1：Few-Shot文本分类
# 设计3个示例，完成电影评论的情感分类（正面/负面/中性）
# ============================================================

print("=" * 60)
print("练习1：Few-Shot文本分类 - 电影评论情感分类")
print("=" * 60)

exercise_1_prompt = """
任务：对电影评论进行情感分类

请根据评论内容，将情感分类为：正面、负面或中性。

---示例---

示例1：
评论："这部电影的剧情紧凑，演员演技在线，特效也很震撼。虽然票价有点贵，但整体来说物超所值，强烈推荐！"
分析：提到多个正面要素（剧情、演技、特效），虽然有小缺点（票价贵），但总体评价积极。
情感分类：正面

示例2：
评论："浪费了两个小时和100块钱，剧情拖沓无聊，演员表演尴尬，中途好几次想离场。这是我今年看过最差的电影。"
分析：全篇充满负面词汇（浪费、拖沓、无聊、尴尬、最差），表达强烈不满。
情感分类：负面

示例3：
评论："电影制作还可以，特效场面不错，但剧情比较平淡，没有太多亮点，也没什么大问题。看完后感觉一般般。"
分析：既有肯定（制作、特效），又指出平淡，整体态度不温不火，没有明显倾向。
情感分类：中性

---现在请分类---

评论："导演的镜头语言很有特色，配乐也很棒，让人印象深刻。这是一部值得细细品味的佳作！"
情感分类：
"""

print(exercise_1_prompt)

# 预期输出说明
print("\n【设计说明】")
print("1. 示例选择原则：")
print("   - 代表性：涵盖正面、负面、中性三种类别")
print("   - 多样性：包含不同的表达方式（强烈/委婉、简单/复杂）")
print("   - 清晰性：每个示例都有明确的分析说明")
print("\n2. 示例特点：")
print("   - 示例1：正面评价，有轻微缺点但整体积极")
print("   - 示例2：强烈负面，用词激烈")
print("   - 示例3：中性态度，有优点也有不足")
print("\n3. 为何有效：")
print("   - 展示了如何处理包含转折的复杂情况")
print("   - 提供了分析过程，帮助LLM理解分类依据")
print("   - 示例格式统一，便于LLM学习模式")


# ============================================================
# 练习2：CoT数学推理
# 用CoT方法解决水池进出水问题
# ============================================================

print("\n\n" + "=" * 60)
print("练习2：CoT数学推理 - 水池进出水问题")
print("=" * 60)

exercise_2_prompt = """
问题：一个水池有3个进水管和2个出水管，进水管每小时进水5升，出水管每小时出水3升，
如果同时打开所有管道，6小时后水池增加多少升水？

让我们一步步分析这个问题：

第一步：理解问题的关键信息
- 进水管数量：3个
- 每个进水管速度：5升/小时
- 出水管数量：2个
- 每个出水管速度：3升/小时
- 运行时间：6小时
- 求：水池净增加的水量

第二步：计算所有进水管的总进水速度
- 3个进水管同时工作
- 总进水速度 = 3 × 5升/小时 = 15升/小时

第三步：计算所有出水管的总出水速度
- 2个出水管同时工作
- 总出水速度 = 2 × 3升/小时 = 6升/小时

第四步：计算每小时净增加的水量
- 净增速度 = 总进水速度 - 总出水速度
- 净增速度 = 15升/小时 - 6升/小时 = 9升/小时
- 这意味着每小时水池实际增加9升水

第五步：计算6小时后的总增量
- 总增量 = 净增速度 × 时间
- 总增量 = 9升/小时 × 6小时 = 54升

第六步：验证答案的合理性
- 6小时总进水：15 × 6 = 90升
- 6小时总出水：6 × 6 = 36升
- 净增加：90 - 36 = 54升 ✓

最终答案：6小时后水池增加54升水。
"""

print(exercise_2_prompt)

print("\n【CoT设计说明】")
print("1. 推理结构：")
print("   - 第一步：理解问题，列出已知条件")
print("   - 第二步：计算总进水速度（分解问题）")
print("   - 第三步：计算总出水速度（分解问题）")
print("   - 第四步：计算净增速度（关键步骤）")
print("   - 第五步：计算最终结果（目标计算）")
print("   - 第六步：验证答案（确保正确）")
print("\n2. 为什么这样设计：")
print("   - 将复杂问题分解为简单的子问题")
print("   - 每一步都有清晰的计算过程")
print("   - 最后验证确保逻辑正确")
print("   - 展示了'分解-计算-组合-验证'的思维模式")


# ============================================================
# 练习3：Few-Shot + CoT组合
# 为"产品评论的问题类型识别"任务设计Few-Shot + CoT
# ============================================================

print("\n\n" + "=" * 60)
print("练习3：Few-Shot + CoT - 产品评论问题类型识别")
print("=" * 60)

exercise_3_prompt = """
任务：识别产品评论中反馈的问题类型，请按照步骤执行

问题类型分类：
1. 功能缺陷：产品功能不正常或不符合预期
2. 质量问题：产品材质、做工、耐用性问题
3. 物流配送：发货慢、包装破损、送错地址等
4. 客服服务：客服态度、响应速度、解决能力
5. 功能建议：希望增加新功能或改进现有功能
6. 使用困惑：不知道如何使用某个功能

分类流程：
1、识别关键词
2、判断问题性质
3、排除其他可能
4、确定类型
5、判断严重程度
6、处理建议

---示例---

示例1：
评论："收到货后发现蓝牙连接总是断断续续的，重启也没用，严重影响使用体验。"

分析思路：
步骤1：识别关键词 → "蓝牙连接"、"断断续续"、"重启也没用"
步骤2：判断问题性质 → 产品核心功能（蓝牙连接）工作不正常
步骤3：排除其他可能 → 不是物流问题、不是使用方法问题、不是建议类
步骤4：确定类型 → 属于产品功能性故障

问题类型：功能缺陷
严重程度：高（影响核心功能）
建议处理：优先级高，需要技术团队排查

---

示例2：
评论："快递包装太简陋了，外箱都烂了，还好产品没坏。希望以后能用气泡膜多包几层。"

分析思路：
步骤1：识别关键词 → "快递包装"、"外箱都烂了"、"气泡膜"
步骤2：判断问题性质 → 与配送过程中的包装有关
步骤3：评估影响范围 → 产品本身未受损，但包装不当存在风险
步骤4：确定类型 → 属于物流配送环节的包装问题

问题类型：物流配送
严重程度：中（产品未损坏但存在隐患）
建议处理：优化包装标准，防止潜在损失

---

示例3：
评论："这个产品的说明书写得太简单了，有些功能根本不知道怎么用，在线教程也找不到，希望能出个详细视频教程。"

分析思路：
步骤1：识别关键词 → "说明书太简单"、"不知道怎么用"、"希望出视频教程"
步骤2：判断问题性质 → 用户能力没问题，但不知道如何操作
步骤3：区分核心诉求 → 主要是使用困惑，次要是功能建议（视频教程）
步骤4：确定类型 → 主要类型是使用困惑，附带功能建议

问题类型：使用困惑（主要） + 功能建议（次要）
严重程度：中（影响用户体验但不影响产品本身）
建议处理：完善产品文档和教程，提升用户引导

---

示例4：
评论："产品本身不错，但是如果能增加定时开关机功能就更完美了，这样可以节省电费。"

分析思路：
步骤1：识别关键词 → "产品本身不错"、"如果能增加"、"定时开关机"
步骤2：判断问题性质 → 现有功能正常，用户提出新需求
步骤3：评估建议价值 → 合理的功能改进建议
步骤4：确定类型 → 属于产品功能建议

问题类型：功能建议
严重程度：低（不影响当前使用）
建议处理：收集需求，纳入产品迭代规划

---现在请分析---

评论："客服回复太慢了，咨询了一个简单问题等了两天才回复，而且回答得也不清楚，最后还是靠自己摸索解决的。"

请按照以下格式输出：

分析思路：
步骤1：[识别关键词]
步骤2：[判断问题性质]
步骤3：[排除或区分其他可能]
步骤4：[确定最终类型]

问题类型：[类型]
严重程度：[高/中/低]
建议处理：[处理建议]
"""

print(exercise_3_prompt)

print("\n【Few-Shot + CoT 设计说明】")
print("1. 组合策略：")
print("   - Few-Shot部分：提供4个不同类型的示例")
print("   - CoT部分：每个示例都包含4步分析思路")
print("   - 输出格式：统一的结构化输出")
print("\n2. 示例覆盖：")
print("   - 示例1：功能缺陷（高严重性）")
print("   - 示例2：物流配送（中严重性）")
print("   - 示例3：使用困惑+建议（复合类型）")
print("   - 示例4：功能建议（低严重性）")
print("\n3. CoT推理模式：")
print("   - 第1步：关键信息提取")
print("   - 第2步：问题性质判断")
print("   - 第3步：多角度验证")
print("   - 第4步：最终分类决策")
print("\n4. 增强要素：")
print("   - 每个示例都有严重程度评估")
print("   - 提供具体的处理建议")
print("   - 展示了处理复合类型问题的方法")


# ============================================================
# 练习4：动态示例选择
# 设计一个方案，根据用户输入动态选择最相关的3个示例
# ============================================================

print("\n\n" + "=" * 60)
print("练习4：动态示例选择方案设计")
print("=" * 60)

print("""
# 动态示例选择方案

## 一、方案概述

**目标**：根据用户输入，从示例库中自动选择最相关的K个示例（默认K=3），
构建定制化的Few-Shot Prompt，提高任务效果并优化Token使用。

**适用场景**：
- 示例库较大（>10个示例）
- 输入多样性高
- 需要优化成本（减少不相关示例的Token消耗）

## 二、技术架构

### 2.1 核心组件

```
┌─────────────────────────────────────────────────────┐
│                    动态示例选择系统                    │
├─────────────────────────────────────────────────────┤
│                                                     │
│  ┌──────────────┐    ┌──────────────┐    ┌────────┐ │
│  │  示例库管理    │ -> │  相似度计算   │ -> │ Prompt │ │
│  │  Vector DB   │    │  Similarity  │    │Builder │ │
│  └──────────────┘    └──────────────┘    └────────┘ │
│         ↑                    ↑                  ↓   │
│         │                    │                  │   │
│  ┌──────────────┐    ┌──────────────┐    ┌────────┐ │
│  │  向量化模块   │    │  用户输入     │     │ LLM    │ │
│  │  Embedding   │    │  User Input  │    │ API    │ │
│  └──────────────┘    └──────────────┘    └────────┘ │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### 2.2 实现流程

```
步骤1：预处理阶段（离线完成）
  ├─ 构建示例库（包含示例文本、标签、元数据）
  ├─ 使用Embedding模型对所有示例进行向量化
  └─ 将向量存储到向量数据库或内存中

步骤2：用户输入阶段（实时）
  ├─ 接收用户输入
  ├─ 对输入进行向量化（使用相同的Embedding模型）
  └─ 计算输入向量与所有示例向量的相似度

步骤3：示例选择阶段
  ├─ 按相似度排序
  ├─ 选择Top-K个最相似的示例
  └─ （可选）应用多样性过滤，避免选择过于相似的示例

步骤4：Prompt构建
  ├─ 将选中的示例按一定顺序排列
  ├─ 构建完整的Few-Shot Prompt
  └─ 添加用户当前输入

步骤5：LLM调用
  └─ 将构建好的Prompt发送给LLM
```

## 三、详细实现方案

### 方案A：基于余弦相似度的简单选择（推荐）

**优点**：实现简单，效果稳定，适合大多数场景

**实现步骤**：

```python
# 伪代码示例

# 1. 准备示例库
example_pool = [
    {"id": 1, "input": "这个产品质量真好", "output": "正面", "embedding": None},
    {"id": 2, "input": "太失望了，不推荐", "output": "负面", "embedding": None},
    {"id": 3, "input": "还行吧，一般般", "output": "中性", "embedding": None},
    # ... 更多示例
]

# 2. 向量化所有示例（使用OpenAI Embedding或其他模型）
from openai import OpenAI
client = OpenAI()

for example in example_pool:
    response = client.embeddings.create(
        model="text-embedding-3-small",
        input=example["input"]
    )
    example["embedding"] = response.data[0].embedding

# 3. 用户输入向量化
user_input = "这个东西非常棒，超出预期"
user_embedding = client.embeddings.create(
    model="text-embedding-3-small",
    input=user_input
).data[0].embedding

# 4. 计算相似度（余弦相似度）
import numpy as np

def cosine_similarity(vec1, vec2):
    return np.dot(vec1, vec2) / (np.linalg.norm(vec1) * np.linalg.norm(vec2))

similarities = []
for example in example_pool:
    sim = cosine_similarity(user_embedding, example["embedding"])
    similarities.append((example, sim))

# 5. 选择Top-K
k = 3
top_k_examples = sorted(similarities, key=lambda x: x[1], reverse=True)[:k]

# 6. 构建Prompt
prompt = "任务：情感分类\\n\\n示例：\\n\\n"
for i, (example, score) in enumerate(top_k_examples, 1):
    prompt += f"示例{i}：\\n"
    prompt += f"输入：{example['input']}\\n"
    prompt += f"输出：{example['output']}\\n\\n"

prompt += f"现在请分类：\\n输入：{user_input}\\n输出："

# 7. 调用LLM
# response = client.chat.completions.create(...)
```

**关键参数**：
- K值：通常选择3-5个，需要根据任务复杂度调整
- 相似度阈值：可以设置最低相似度阈值，过滤不相关示例

""")


# ============================================================
# 总结
# ============================================================

print("\n\n" + "=" * 60)
print("练习总结")
print("=" * 60)

print("""
本讲的4个练习涵盖了Few-Shot与CoT的核心应用：

1. 练习1 - Few-Shot文本分类
   核心：设计高质量示例，覆盖不同类别和边界情况
   
2. 练习2 - CoT数学推理
   核心：分步推理，将复杂问题分解为简单步骤
   
3. 练习3 - Few-Shot + CoT组合
   核心：结合示例和推理，处理复杂的分类任务
   
4. 练习4 - 动态示例选择
   核心：根据输入自动选择最相关示例，优化效果和成本

关键要点：
✓ Few-Shot的核心是示例的代表性和多样性
✓ CoT的核心是清晰的推理步骤和逻辑链
✓ 组合使用时要确保格式统一和逻辑一致
✓ 动态选择能大幅提升实际应用的效果和效率

实践建议：
1. 先从固定的2-3个高质量示例开始
2. 测试Zero-Shot、Few-Shot、Few-Shot+CoT的效果差异
3. 根据任务复杂度选择合适的技术组合
4. 在示例库较大时，考虑实施动态选择策略
5. 持续评估和优化示例质量

下一讲预告：
《讲06 | 高级Prompt技术》将介绍ReAct、Tree of Thoughts、
Self-Refine等更高级的提示词工程技术！
""")

print("\n" + "=" * 60)
print("练习完成！建议动手实践每个方案，加深理解。")
print("=" * 60)


